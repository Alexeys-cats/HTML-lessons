name: Feature Branch Preview

on:
  push:
    branches-ignore:
      - main # Исключаем main, так как она обрабатывается в отдельном пайплайне
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write # Необходимо для публикации на GitHub Pages
  pull-requests: write # Необходимо для комментирования PR

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Get branch name
        id: branch-name
        uses: tj-actions/branch-names@v7

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build:ci
        env:
          # Создаем уникальную директорию для каждой ветки
          VITE_BASE_PATH: /HTML-lessons/preview/${{ steps.branch-name.outputs.current_branch }}

      # Добавляем базовый путь в настройки сборки
      - name: Update base path in HTML
        run: |
          # Создаем .nojekyll для GitHub Pages
          touch dist/.nojekyll
          
          # Обновляем базовый путь в index.html, если это необходимо
          BRANCH_PATH=$(echo ${{ steps.branch-name.outputs.current_branch }} | sed 's/\//-/g')
          sed -i "s|<base href=\"/HTML-lessons/\">|<base href=\"/HTML-lessons/preview/${BRANCH_PATH}/\">|" dist/index.html || true

      # Деплой на GitHub Pages в отдельную директорию для ветки
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          target-folder: preview/${{ steps.branch-name.outputs.current_branch }}
          clean: true # Удаляем только файлы в папке текущей ветки

      # Добавляем комментарий в PR с ссылкой на фича-стенд
      - name: Find Pull Request
        uses: jwalton/gh-find-current-pr@v1
        id: find-pr
        if: github.event_name == 'push' || github.event_name == 'pull_request'
        with:
          state: open

      - name: Add or update comment with preview link
        if: steps.find-pr.outputs.number
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          number: ${{ steps.find-pr.outputs.number }}
          header: preview
          message: |
            🚀 **Превью фича-стенда готово!**
            
            Проверить изменения можно по ссылке: [Фича-стенд на GitHub Pages](https://${{ github.repository_owner }}.github.io/HTML-lessons/preview/${{ steps.branch-name.outputs.current_branch }}/)
            
            _Автоматически создано из ветки: ${{ steps.branch-name.outputs.current_branch }}_
